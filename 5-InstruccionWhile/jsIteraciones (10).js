function mostrar()
{
	var num;	
	var contneg=0;



	var contador=0;
	//declarar contadores y variables 

}
	while(respuesta!="no")
	{ num = prompt ("ingrese el numero");
	num = parseint (num);

	while (ISNAN(num)) {
		num = prompt ();
		num = parseint(num);
	}

	if ( num < 0 ) {
		contneg++; 
		
	
	}




}//FIN DE LA FUNCIÓN