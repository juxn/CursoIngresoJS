function mostrar()
{

	var contador=0;
	// declarar variables
	var max;
	var min;
	var num;
	var bandera=true;

	var respuesta='si';
	while (respuesta = 'si') {
		num = prompt("ingrese nùmero")

		if ( bandera ) {
			bandera = false;
			max=num;
			min=num;
		} else {
			if (num > max )
			max = num;
			if (num < min)
			min = num;
		}

}
	while(respuesta!='no')
	{ 
		
	
	}




}//FIN DE LA FUNCIÓN